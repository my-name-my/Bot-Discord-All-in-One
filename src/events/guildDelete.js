/** guildDelete — giu kho luu tru gon: config duoc giu lai (moi lai giu nguyen cai dat). */
module.exports = {
  name: 'guildDelete',
  async execute(client, guild) {
    // Co y KHONG xoa config server: neu moi bot lai, cai dat van giu nguyen.
    // Co the xoa thu cong bang /config reset.
  },
};
